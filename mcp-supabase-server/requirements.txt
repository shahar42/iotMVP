# MCP Supabase Server - Enhanced with FastMCP and PostgreSQL optimizations
# (SQLAlchemy, psycopg2-binary, python-dotenv already available in parent project)

# Core MCP framework
mcp[cli]>=1.1.0
fastmcp>=0.1.0

# PostgreSQL async support and enhanced connectivity
asyncpg>=0.29.0
httpx>=0.27.0

# Enhanced validation and serialization (compatible with MCP requirements)
pydantic>=2.8.0